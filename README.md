# ToTheMoon i18n Translator

> Batch translation tool for UI localization JSON files using OpenAI GPT. Dual-reference strategy (English + Russian) for better context, translates to 19+ languages. Python 3.11+, Poetry, caching and batching.

Автоматический переводчик UI-текстов с использованием искусственного интеллекта (GPT). 

**Главная фишка:** Использует **два языка** (английский + русский) для понимания контекста и создания качественных переводов.

## 📖 Содержание

- [🚀 Первый запуск за 5 минут](#-первый-запуск-за-5-минут) — Начните здесь!
- [🔄 Что делать дальше?](#-что-делать-дальше) — Обновления и переводы
- [🌍 Массовый перевод на все языки](#-массовый-перевод-на-все-языки) — 19 языков одной командой
- [💡 Полезные команды](#-полезные-команды) — Быстрый доступ
- [❓ FAQ](#-faq-частые-вопросы) — Ответы на вопросы
- [🐛 Что-то не работает?](#-что-то-не-работает) — Troubleshooting
- [🎯 Для разработчиков](#-для-разработчиков) — Техническая документация
- [⚡ Quick Reference](#-quick-reference-шпаргалка) — Шпаргалка команд

---

## 🚀 Первый запуск за 5 минут

### Шаг 1: Установка

```bash
# 1. Склонируйте проект (если ещё не сделали)
git clone <repo-url>
cd tothemoon-i18n

# 2. Установите зависимости (займёт ~2 минуты)
make install
```

**Что происходит:** Устанавливается Python-окружение и все необходимые библиотеки.

---

### Шаг 2: Настройка OpenAI API ключа

#### 2.1. Получите API ключ

1. Зайдите на https://platform.openai.com/api-keys
2. Нажмите "Create new secret key"
3. Скопируйте ключ (начинается с `sk-...`)

#### 2.2. Добавьте баланс (ВАЖНО!)

⚠️ **Без этого не будет работать!**

1. Откройте https://platform.openai.com/settings/organization/billing/overview
2. Нажмите "Add credits" и добавьте минимум **$5-10**
3. Убедитесь что "Prepaid credits" показывает баланс

💰 **Стоимость:** Полный перевод ~4,000 строк = **~$0.30** (30 минут)

#### 2.3. Создайте .env файл

```bash
# Скопируйте этот код в терминал (замените YOUR-KEY на ваш ключ!)
cat > .env << 'EOF'
# OpenAI API настройки (обязательно!)
OPENAI_API_KEY=sk-YOUR-KEY-HERE

# Модель и базовые настройки
OPENAI_MODEL=gpt-4.1-mini
OPENAI_MAX_RETRIES=3
OPENAI_TIMEOUT_SECONDS=60
OPENAI_TEMPERATURE=0.3

# Оптимизация для gpt-4.1-mini (быстро + недорого)
TRANSLATION_BATCH_SIZE=25
TRANSLATION_BATCH_MAX_ATTEMPTS=2
TRANSLATION_MIN_DELAY_SECONDS=0.5
TRANSLATION_RETRY_DELAY_SECONDS=5.0

# Целевой язык (по умолчанию)
TRANSLATION_LOCALE=ru
EOF

# Откройте файл и замените YOUR-KEY на ваш настоящий ключ
nano .env  # или используйте любой редактор
```

---

### Шаг 3: Проверка что всё работает

```bash
# Запустите проверку
make check
```

**Ожидаемый результат:**
```
✓ Code formatting (ruff format)
✓ Code linting (ruff check)  
✓ Type checking (mypy)
✓ Tests (pytest) - 57 passed
✓ i18n validation
```

❌ **Если что-то упало** - смотрите секцию ["Что-то не работает?"](#-что-то-не-работает)

---

### Шаг 4: Первый перевод (EN → RU)

**Убедитесь что у вас есть файл `data/TTM_EN.json`** (английские тексты)

```bash
# Запустите перевод на русский
make translate-ru
```

**Что вы увидите:**

```
[INFO] Starting translation pipeline
[INFO] Source: data/TTM_EN.json (3991 keys)
[INFO] Target: data/TTM_RU.json
[INFO] Model: gpt-4.1-mini

Processing batches: 100%|████████████| 160/160 [27:34<00:00, 10.3s/batch]

[INFO] Translation completed successfully!
[INFO] Translated: 3991 keys
[INFO] Cache hits: 0 (first run)
[INFO] Time: 27m 34s
[INFO] Estimated cost: ~$0.30

✓ Validation passed: All keys and placeholders match
✓ File saved: data/TTM_RU.json
```

**Время:** ~30 минут для 4,000 строк  
**Стоимость:** ~$0.30

---

### Шаг 5: Проверка результата

```bash
# Проверьте что перевод корректный
make lint-i18n SOURCE_EN=data/TTM_EN.json OUT_DST=data/TTM_RU.json
```

**Ожидаемый результат:**
```
✓ Key parity check passed (3991/3991 keys)
✓ Placeholder validation passed
✓ No empty translations found
✓ All checks passed!
```

**Готово!** 🎉 Теперь у вас есть файл `data/TTM_RU.json` с русским переводом!

---

## 🔄 Что делать дальше?

### Сценарий 1: Добавились новые тексты в English

```bash
# 1. Обновите data/TTM_EN.json (добавьте новые ключи)

# 2. Запустите перевод (переведутся только НОВЫЕ ключи!)
make translate-ru

# 3. Проверьте
make lint-i18n SOURCE_EN=data/TTM_EN.json OUT_DST=data/TTM_RU.json
```

**Время:** ~1-3 минуты (благодаря кэшу)  
**Стоимость:** ~$0.01

### Сценарий 2: Перевод на другие языки (ES, DE, FR...)

После того как у вас есть **качественный** `TTM_RU.json`:

```bash
# Испанский (использует EN + RU для лучшего качества!)
make translate LOCALE=es OUT_DST=data/TTM_ES.json

# Немецкий
make translate LOCALE=de OUT_DST=data/TTM_DE.json

# Французский
make translate LOCALE=fr OUT_DST=data/TTM_FR.json
```

**Почему это лучше:** LLM видит два языка и лучше понимает контекст UI (кнопки, заголовки, ошибки).

### Сценарий 3: Проверка кэша

```bash
# Посмотрите статистику кэша
make cache-stats
```

**Вывод:**
```
Translation Cache Statistics:
  File: .translation_cache.json
  Size: 2.1M
  Cached translations: 3991
  Model version: gpt-4.1-mini:v1
```

---

## 🌍 Массовый перевод на все языки

### Быстрый старт: Одна команда для 19 языков

После создания качественного `TTM_RU.json`, можно автоматически перевести на все поддерживаемые языки:

```bash
./translate_all.sh
```

**Что произойдёт:**
- ✅ Переведёт 19 языков автоматически
- ✅ Пропустит уже существующие файлы
- ✅ Покажет прогресс в реальном времени
- ✅ Можно перезапустить если упадёт — продолжит с места остановки

**Время:** ~3-4 часа с `gpt-4.1-mini`  
**Стоимость:** ~$10-12 за все 19 языков

### Поддерживаемые языки

| Язык | Код | Файл |
|------|-----|------|
| 🇧🇬 Bulgarian | `bg` | `TTM_BG.json` |
| 🇨🇳 Chinese (Simplified) | `zh-Hans` | `TTM_ZH_HANS.json` |
| 🇳🇱 Dutch | `nl` | `TTM_NL.json` |
| 🇫🇷 French | `fr` | `TTM_FR.json` |
| 🇩🇪 German | `de` | `TTM_DE.json` |
| 🇮🇳 Hindi | `hi` | `TTM_HI.json` |
| 🇮🇩 Indonesian | `id` | `TTM_ID.json` |
| 🇯🇵 Japanese | `ja` | `TTM_JA.json` |
| 🇰🇷 Korean | `ko` | `TTM_KO.json` |
| 🇲🇾 Malay | `ms` | `TTM_MS.json` |
| 🇵🇱 Polish | `pl` | `TTM_PL.json` |
| 🇵🇹 Portuguese | `pt` | `TTM_PT.json` |
| 🇧🇷 Portuguese (Brazil) | `pt-br` | `TTM_PT_BR.json` |
| 🇪🇸 Spanish | `es` | `TTM_ES.json` |
| 🇵🇭 Tagalog | `tl` | `TTM_TL.json` |
| 🇹🇭 Thai | `th` | `TTM_TH.json` |
| 🇹🇷 Turkish | `tr` | `TTM_TR.json` |
| 🇺🇦 Ukrainian | `uk` | `TTM_UK.json` |
| 🇻🇳 Vietnamese | `vi` | `TTM_VI.json` |

### Индивидуальные команды

Если нужен только один конкретный язык:

```bash
# Французский
make translate LOCALE=fr OUT_DST=data/TTM_FR.json

# Немецкий
make translate LOCALE=de OUT_DST=data/TTM_DE.json

# Японский
make translate LOCALE=ja OUT_DST=data/TTM_JA.json

# Китайский (упрощенный)
make translate LOCALE=zh-Hans OUT_DST=data/TTM_ZH_HANS.json
```

### Мониторинг прогресса

Откройте второй терминал и запустите:

```bash
# Показать прогресс
while true; do 
    clear
    echo "=== Translation Progress ==="
    echo "Files: $(ls data/TTM_*.json 2>/dev/null | wc -l)/21"
    echo "Cache: $(wc -l < .translation_cache.json 2>/dev/null || echo 0) entries"
    sleep 10
done
```

### Проверка всех переводов

```bash
# Валидация всех языков сразу
for file in data/TTM_*.json; do
    if [ "$file" != "data/TTM_EN.json" ]; then
        echo "Checking $file..."
        make lint-i18n SOURCE_EN=data/TTM_EN.json OUT_DST="$file" || true
    fi
done
```

---

## 💡 Полезные команды

```bash
# Посмотреть все доступные команды
make help

# Очистить кэш (начать с чистого листа)
make clean

# Проверить весь проект (lint, tests, validation)
make check

# Перевести на конкретный язык
make translate LOCALE=es OUT_DST=data/TTM_ES.json

# Посмотреть статистику кэша
make cache-stats
```

---

## ❓ FAQ (Частые вопросы)

### Q: Сколько это стоит?

**A:** Для ~4,000 строк:
- **Один язык (EN→RU):** ~$0.30 (30 минут)
- **Все 19 языков:** ~$10-12 (3-4 часа с `gpt-4.1-mini`)
- **Обновление 10% текстов:** ~$0.03 на язык (3 минуты)
- **Повторный запуск без изменений:** $0 (мгновенно, из кэша)

**Создание глоссариев (опционально):** ~$0.40 за все 19 языков

### Q: Что если у меня нет русского файла?

**A:** Просто запустите:
```bash
make translate-ru
```
Система автоматически создаст `data/TTM_RU.json` из `data/TTM_EN.json`.

### Q: Можно ли переводить сразу на испанский без русского?

**A:** Можно, но **качество будет хуже**:
```bash
make translate LOCALE=es SOURCE_RU= OUT_DST=data/TTM_ES.json
```

**Почему хуже?** Система использует **dual-reference mode** — показывает LLM оба языка (EN + RU) для лучшего понимания контекста:
```json
{
  "key": "common.button:next",
  "en": "Next",        // Английский
  "ru": "Далее",       // Русский (видит что на кнопках короткие слова!)
  "intent": "button"
}
→ Французский: "Suivant" (тоже короткое слово для кнопки)
```

**Рекомендация:** Сначала создайте качественный `TTM_RU.json`, затем используйте его для других языков.

### Q: Как перевести сразу на все языки?

**A:** Одной командой:
```bash
./translate_all.sh
```

Скрипт автоматически переведет 19 языков, займёт ~1-3 часа и будет стоить ~$5-6 с `gpt-4.1-mini`.

### Q: Что делать если перевод прервался на половине?

**A:** Просто запустите команду снова:
```bash
make translate-ru
# или
./translate_all.sh
```

Система автоматически **продолжит с места остановки** (благодаря кэшу). Вы не потеряете прогресс!

### Q: Как проверить что перевод качественный?

**A:** 
1. Автоматическая проверка: `make lint-i18n`
2. Ручная проверка: откройте `data/TTM_RU.json` и проверьте ключевые фразы (кнопки, ошибки, заголовки)

### Q: Можно ли использовать другие модели GPT?

**A:** Да, отредактируйте `.env`:

```env
# Для максимального качества (медленнее и дороже)
OPENAI_MODEL=gpt-5
OPENAI_TIMEOUT_SECONDS=300
TRANSLATION_BATCH_SIZE=40
TRANSLATION_MIN_DELAY_SECONDS=3.0
```

**Сравнение моделей:**

| Модель | Время (4K строк) | Стоимость | Качество |
|--------|-----------------|-----------|----------|
| **gpt-4.1-mini** ⭐ | 30 мин | $0.30 | Отлично |
| **gpt-5** | 2 часа | $9.00 | Отлично+ |

**Рекомендация:** `gpt-4.1-mini` для production.

### Q: Что такое "placeholder" и почему они важны?

**A:** Placeholders - это переменные в тексте:
```
"Welcome, {username}!" → "Добро пожаловать, {username}!"
```

Система **автоматически защищает** их, чтобы они не переводились. Если валидация ругается на placeholders - это важная проблема!

### Q: Нужно ли коммитить `.translation_cache.json`?

**A:** 
- ✅ **Да, если работаете в команде** - все будут использовать общий кэш
- ❌ **Нет, если работаете один** - можно добавить в `.gitignore`

---

## 🐛 Что-то не работает?

### Ошибка: `insufficient_quota`

```
Error: OpenAI rate limit or quota exceeded
```

**Решение:**
1. Откройте https://platform.openai.com/settings/organization/billing/overview
2. Проверьте "Prepaid credits" - должно быть > $0
3. Добавьте минимум $5-10

### Ошибка: `make: poetry: No such file or directory`

**Решение:**
```bash
# Установите Poetry
curl -sSL https://install.python-poetry.org | python3 -

# Или через pip
pip install poetry
```

### Ошибка: `Rate limit exceeded (429)`

**Решение:** Увеличьте задержки в `.env`:
```env
TRANSLATION_MIN_DELAY_SECONDS=2.0
TRANSLATION_RETRY_DELAY_SECONDS=10.0
```

### Ошибка: `Placeholder mismatch`

```
[fail] pages.landing:title: placeholder mismatch (missing ×1)
```

**Решение:** Это редко происходит (система защищает placeholders автоматически). Если всё же случилось:
1. Проверьте `data/TTM_EN.json` - возможно там placeholder value: `"key": "key"`
2. Эти значения копируются **автоматически** без перевода (с версии 2.1)
3. Просто запустите перевод снова - система сама исправит

### Ошибка: `Command timed out`

**Решение:** Уменьшите batch size в `.env`:
```env
TRANSLATION_BATCH_SIZE=15
```

### Ошибка: `File not found: data/TTM_EN.json`

**Решение:** Убедитесь что у вас есть исходный английский файл:
```bash
ls -la data/TTM_EN.json
```

Если файла нет - создайте его или укажите правильный путь:
```bash
make translate-ru SOURCE_EN=путь/к/вашему/файлу.json
```

---

## 🎯 Для разработчиков

### Архитектура

```
tools/
├── ttm_translate.py     # CLI точка входа
├── pipeline.py          # Основная логика перевода
├── provider_openai.py   # OpenAI клиент
├── protect.py           # Защита placeholders
├── validators.py        # Валидация
└── batcher.py           # Батчинг и интент-распознавание
```

### CLI параметры

```bash
poetry run python tools/ttm_translate.py \
  --source-en data/TTM_EN.json \
  --source-ru data/TTM_RU.json \
  --dst data/TTM_ES.json \
  --locale es \
  --batch-size 25 \
  --min-delay 0.5
```

**Основные параметры:**
- `--source-en PATH` - английский файл (обязательно)
- `--source-ru PATH` - русский файл (опционально, улучшает качество)
- `--dst PATH` - куда сохранить результат (обязательно)
- `--locale CODE` - целевой язык (`ru`, `es`, `de`, `fr`, `zh-cn`, и т.д.)
- `--draft PATH` - существующий перевод для использования как подсказки
- `--single-reference-mode` - режим с одним эталоном (только EN)

**Производительность:**
- `--batch-size N` - ключей в одном запросе (рекомендуется 25)
- `--min-delay SECONDS` - задержка между запросами (0.5 для gpt-4.1-mini)
- `--retry-delay SECONDS` - задержка перед повтором при ошибке (5.0)
- `--no-progress` - отключить прогресс-бар

**Кэширование:**
- `--cache-file PATH` - путь к кэшу (по умолчанию `.translation_cache.json`)
- `--no-cache` - отключить кэш (переводить всё заново)

### Добавление нового языка

```bash
# 1. (Опционально) Создайте глоссарий и few-shot примеры
make bootstrap-locale LOCALE=zh-Hans TARGET_LANG="Chinese (Simplified)"

# 2. (Опционально) Отредактируйте сгенерированные файлы
#    - glossary/zh-Hans.yml      (термины: Staking → 质押)
#    - prompts/fewshot/zh-Hans.json (примеры переводов)

# 3. Запустите перевод (работает и без глоссария!)
make translate LOCALE=zh-Hans OUT_DST=data/TTM_ZH_HANS.json

# 4. Проверьте результат
make lint-i18n SOURCE_EN=data/TTM_EN.json OUT_DST=data/TTM_ZH_HANS.json
```

**Важно:** Глоссарии опциональны! Система отлично переводит благодаря dual-reference mode (EN + RU).

#### Создание глоссариев для всех языков

```bash
# Если хотите улучшить качество через глоссарии
make bootstrap-locale LOCALE=bg TARGET_LANG="Bulgarian"
make bootstrap-locale LOCALE=fr TARGET_LANG="French"
make bootstrap-locale LOCALE=de TARGET_LANG="German"
make bootstrap-locale LOCALE=ja TARGET_LANG="Japanese"
# ... и т.д. для всех языков
```

#### Перегенерация существующих глоссариев

```bash
# Если глоссарий уже существует, добавьте FORCE=1
make bootstrap-locale LOCALE=fr TARGET_LANG="French" FORCE=1
```

### Тестирование

```bash
# Все тесты
make test

# Полная проверка (lint + typecheck + tests + i18n)
make check

# Форматирование кода
make format
```

### Добавление своего LLM-провайдера

Реализуйте интерфейс `LLMClient` из `tools/interfaces.py`:

```python
# tools/provider_anthropic.py
from tools.interfaces import LLMClient, Message
from typing import Dict, Iterable

class AnthropicClient:
    @property
    def model(self) -> str:
        return "claude-3-sonnet"
    
    def complete(self, messages: Iterable[Message]) -> Dict[str, str]:
        # Ваша реализация
        pass
```

### Переменные окружения

Полный список переменных в `.env`:

```env
# OpenAI (обязательно)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4.1-mini
OPENAI_MAX_RETRIES=3
OPENAI_TIMEOUT_SECONDS=60
OPENAI_TEMPERATURE=0.3

# Pipeline
TRANSLATION_LOCALE=ru
TRANSLATION_BATCH_SIZE=25
TRANSLATION_BATCH_MAX_ATTEMPTS=2
TRANSLATION_MIN_DELAY_SECONDS=0.5
TRANSLATION_RETRY_DELAY_SECONDS=5.0
```

---

## 📊 Производительность

### Реальные метрики (3,991 ключ)

| Модель | Batch Size | Время | Стоимость | Качество |
|--------|-----------|-------|-----------|----------|
| **gpt-4.1-mini** ⭐ | 25 | 30 мин | $0.30 | Отлично |
| **gpt-5** | 40 | 2 часа | $9.00 | Отлично+ |

### Кэширование

| Сценарий | Время | API-вызовы | Экономия |
|----------|-------|------------|----------|
| Первый запуск | 30 мин | ~160 | - |
| Обновление 10% | 3 мин | ~16 | **10x** |
| Без изменений | 1 сек | 0 | **100x** |

---

## ⚡ Quick Reference (Шпаргалка)

### Основные команды

```bash
# Перевести все языки сразу (19 языков)
./translate_all.sh

# Первый EN→RU перевод
make translate-ru

# Перевести на конкретный язык
make translate LOCALE=fr OUT_DST=data/TTM_FR.json

# Проверить качество
make lint-i18n SOURCE_EN=data/TTM_EN.json OUT_DST=data/TTM_FR.json

# Статистика кэша
make cache-stats

# Все проверки (lint + tests + validation)
make check
```

### Глоссарии (опционально)

```bash
# Создать глоссарий
make bootstrap-locale LOCALE=fr TARGET_LANG="French"

# Перегенерировать существующий
make bootstrap-locale LOCALE=fr TARGET_LANG="French" FORCE=1
```

### Мониторинг

```bash
# Количество файлов
ls data/TTM_*.json | wc -l

# Размер кэша
wc -l < .translation_cache.json

# Реал-тайм прогресс
while true; do 
    clear
    echo "Files: $(ls data/TTM_*.json | wc -l)/21"
    echo "Cache: $(wc -l < .translation_cache.json) entries"
    sleep 5
done
```

### Частые ошибки и решения

| Ошибка | Решение |
|--------|---------|
| `insufficient_quota` | Пополните баланс OpenAI |
| `429 Too Many Requests` | Уменьшите `TRANSLATION_BATCH_SIZE=15` |
| `Files already exist` (bootstrap) | Добавьте `FORCE=1` |
| Скрипт упал | Запустите снова — продолжит с места остановки |

---

## 📚 Дополнительная документация

- **[config/languages.yml](config/languages.yml)** - Справочник поддерживаемых языков
- **[prompts/system.txt](prompts/system.txt)** - Системный промпт для LLM
- **[glossary/default.yml](glossary/default.yml)** - Базовый глоссарий терминов

---

## 🤝 Контрибьюция

1. Fork репозитория
2. Создайте feature-ветку: `git checkout -b feature/amazing-feature`
3. Закоммитьте изменения: `git commit -m 'feat: add amazing feature'`
4. Push в ветку: `git push origin feature/amazing-feature`
5. Откройте Pull Request

**Перед отправкой PR:**
```bash
make check  # Все тесты должны пройти ✓
```

---

## 📝 Лицензия

MIT

---

## 🆘 Поддержка

Если не нашли ответ в FAQ:

1. Проверьте [Issues](../../issues) - возможно решение уже есть
2. Запустите `make check` для диагностики
3. Создайте новый Issue с подробным описанием и логами

---

**Удачи с переводами! 🚀**
